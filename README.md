---
title: AlzheimerEnv
emoji: 🧠
colorFrom: blue
colorTo: green
sdk: docker
pinned: false
---

# AlzheimerEnv — OpenEnv Submission

A real-world OpenEnv RL environment for Alzheimer's disease prediction and gene expression intervention planning.

## API Endpoints
- `GET /health` — Health check
- `POST /reset` — Reset environment
- `POST /step` — Take action
- `GET /state` — Current state
- `GET /tasks` — List all tasks
